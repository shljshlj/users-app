import { Box } from "@chakra-ui/react"

const ContentSection = ({ children }) => {
  return (
    <Box
    // px={4}
    // my="2rem"
    >
      {children}
    </Box>
  );
};

export default ContentSection;